VITE_API_URL=http://localhost:3000/api/v1
VITE_SENTRY_DSN=https://96afa5eacdb25e5bcb8b4142f8a09a3e@o4511814544130048.ingest.de.sentry.io/4511835342045264

# AI menu features (OCR + allergy-aware search). Off by default: the backend
# routes return "not configured" without ANTHROPIC_API_KEY, so showing the UI
# would give customers a search box that always fails. Set to true ONLY
# alongside a funded backend key. Build-time value — needs a rebuild.
VITE_AI_ENABLED=false
